{
  "name": "loom-session-tracker",
  "displayName": "Loom Session Tracker",
  "description": "Live per-project map of Loom session agents (role \u2194 webviewId) over CDP. Shows the orchestrator session so you can tag it in one click; tagged, it is auto-notified whenever a worker finishes.",
  "version": "0.14.0",
  "publisher": "local",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "loomSessionTracker",
          "title": "Loom Sessions",
          "icon": "$(broadcast)"
        }
      ]
    },
    "views": {
      "loomSessionTracker": [
        {
          "id": "loomSessions",
          "name": "Session Agents"
        }
      ]
    },
    "commands": [
      {
        "command": "loomSessionTracker.refresh",
        "title": "Loom Sessions: Refresh Now",
        "icon": "$(sync)"
      },
      {
        "command": "loomSessionTracker.status",
        "title": "Loom Sessions: Show Status"
      },
      {
        "command": "loomSessionTracker.spawn",
        "title": "Loom Sessions: Spawn Session\u2026",
        "icon": "$(add)"
      },
      {
        "command": "loomSessionTracker.retire",
        "title": "Loom Sessions: Retire (Close) Session",
        "icon": "$(close)"
      },
      {
        "command": "loomSessionTracker.delete",
        "title": "Loom Sessions: Delete Session (worktree + transcript)",
        "icon": "$(trash)"
      },
      {
        "command": "loomSessionTracker.lock",
        "title": "Loom Sessions: Lock (protect from deletion)",
        "icon": "$(lock)"
      },
      {
        "command": "loomSessionTracker.unlock",
        "title": "Loom Sessions: Unlock",
        "icon": "$(unlock)"
      },
      {
        "command": "loomSessionTracker.tagOrchestrator",
        "title": "Loom Sessions: Tag as Orchestrator",
        "icon": "$(star-full)"
      },
      {
        "command": "loomSessionTracker.untagOrchestrator",
        "title": "Loom Sessions: Untag Orchestrator",
        "icon": "$(star-empty)"
      },
      {
        "command": "loomSessionTracker.sessionCount",
        "title": "Loom Sessions: Show Simultaneous Session Count",
        "icon": "$(pulse)"
      },
      {
        "command": "loomSessionTracker.digest",
        "title": "Loom Sessions: What Needs Me? (startup digest)",
        "icon": "$(checklist)"
      },
      {
        "command": "loomSessionTracker.toggleAllProjects",
        "title": "Loom Sessions: Toggle All-Projects View",
        "icon": "$(globe)"
      },
      {
        "command": "loomSessionTracker.worktreeReport",
        "title": "Loom Sessions: Worktree Cleanup Report",
        "icon": "$(trash)"
      },
      {
        "command": "loomSessionTracker.bankContext",
        "title": "Loom Sessions: Bank Orchestrator Memory & Clear Context",
        "icon": "$(save)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "loomSessionTracker.spawn",
          "when": "view == loomSessions",
          "group": "navigation@1"
        },
        {
          "command": "loomSessionTracker.tagOrchestrator",
          "when": "view == loomSessions",
          "group": "navigation@2"
        },
        {
          "command": "loomSessionTracker.refresh",
          "when": "view == loomSessions",
          "group": "navigation@3"
        },
        {
          "command": "loomSessionTracker.sessionCount",
          "when": "view == loomSessions",
          "group": "navigation@4"
        },
        {
          "command": "loomSessionTracker.digest",
          "when": "view == loomSessions",
          "group": "navigation@5"
        },
        {
          "command": "loomSessionTracker.toggleAllProjects",
          "when": "view == loomSessions",
          "group": "navigation@6"
        },
        {
          "command": "loomSessionTracker.bankContext",
          "when": "view == loomSessions",
          "group": "navigation@7"
        }
      ],
      "view/item/context": [
        {
          "command": "loomSessionTracker.lock",
          "when": "view == loomSessions && viewItem == loomAgent",
          "group": "inline@1"
        },
        {
          "command": "loomSessionTracker.retire",
          "when": "view == loomSessions && viewItem == loomAgent",
          "group": "inline@2"
        },
        {
          "command": "loomSessionTracker.unlock",
          "when": "view == loomSessions && viewItem == loomAgentLocked",
          "group": "inline@1"
        },
        {
          "command": "loomSessionTracker.lock",
          "when": "view == loomSessions && viewItem == loomAgent",
          "group": "1_actions"
        },
        {
          "command": "loomSessionTracker.unlock",
          "when": "view == loomSessions && viewItem == loomAgentLocked",
          "group": "1_actions"
        },
        {
          "command": "loomSessionTracker.retire",
          "when": "view == loomSessions && viewItem == loomAgent",
          "group": "1_actions"
        },
        {
          "command": "loomSessionTracker.tagOrchestrator",
          "when": "view == loomSessions && viewItem =~ /loomAgent/",
          "group": "1_actions"
        },
        {
          "command": "loomSessionTracker.untagOrchestrator",
          "when": "view == loomSessions && viewItem == loomOrchestrator",
          "group": "inline@1"
        },
        {
          "command": "loomSessionTracker.untagOrchestrator",
          "when": "view == loomSessions && viewItem == loomOrchestrator",
          "group": "1_actions"
        },
        {
          "command": "loomSessionTracker.delete",
          "when": "view == loomSessions && viewItem == loomAgent",
          "group": "2_destructive"
        },
        {
          "command": "loomSessionTracker.tagOrchestrator",
          "when": "view == loomSessions && viewItem == loomOwnerCandidate",
          "group": "inline@1"
        },
        {
          "command": "loomSessionTracker.tagOrchestrator",
          "when": "view == loomSessions && viewItem == loomOwnerCandidate",
          "group": "1_actions"
        }
      ]
    },
    "configuration": {
      "title": "Loom Session Tracker",
      "properties": {
        "loomSessionTracker.intervalMs": {
          "type": "number",
          "default": 15000,
          "minimum": 5000,
          "description": "How often (ms) to refresh the session-agent map. Clamped to a 5s floor."
        },
        "loomSessionTracker.notifyOrchestrator": {
          "type": "boolean",
          "default": true,
          "description": "When a worker's status.json goes working \u2192 idle/blocked, inject a 'check the outbox' prompt into the tagged orchestrator session."
        },
        "loomSessionTracker.sessionWarnThreshold": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "description": "Warn (status-bar highlight) when this many Claude conversations are open simultaneously across all windows. Anthropic sets no hard cap, but all sessions share one usage pool; its own guidance suggests 3-5 in parallel."
        },
        "loomSessionTracker.autoResumeAfterLimit": {
          "type": "boolean",
          "default": true,
          "description": "When a session is blocked by a usage limit, automatically nudge it to continue once the limit lifts (detected by the limit banner clearing, not by the UI's coarse ETA)."
        },
        "loomSessionTracker.resumeMessage": {
          "type": "string",
          "default": "",
          "description": "Prompt injected to resume a session after its usage limit resets. Empty = the built-in loom-resume message."
        },
        "loomSessionTracker.enforceWorkerModel": {
          "type": "boolean",
          "default": true,
          "description": "Keep the most expensive model for the orchestrator only. A worker found on a premium model is switched back with /model <workerModel>."
        },
        "loomSessionTracker.workerModel": {
          "type": "string",
          "default": "claude-opus-5",
          "description": "Model id workers are switched to (passed to /model). The orchestrator is exempt."
        },
        "loomSessionTracker.premiumModels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "Fable 5.1",
            "Fable 5",
            "Mythos 5.1",
            "Mythos 5"
          ],
          "description": "Display names of the top pricing tier ($10/$50 per MTok) reserved for the orchestrator."
        },
        "loomSessionTracker.showStartupDigest": {
          "type": "boolean",
          "default": true,
          "description": "On startup, summarise what needs attention: responses not picked up, roles blocked on a decision or by a usage limit, workers on the premium model, unbanked worktree changes, roles whose session is not open, and whether an orchestrator is tagged."
        },
        "loomSessionTracker.staleBusDays": {
          "type": "number",
          "default": 30,
          "minimum": 1,
          "description": "Flag a project bus as stale in the digest when nothing has touched it for this many days."
        },
        "loomSessionTracker.digestUnbankedCheck": {
          "type": "boolean",
          "default": true,
          "description": "Include a git status check of each role's worktree in the digest (runs at startup and on demand, not every tick)."
        },
        "loomSessionTracker.stallWatchdog": {
          "type": "boolean",
          "default": true,
          "description": "Warn (and tell the orchestrator) when a role has been 'working' with no status update for stallMinutes. The finish notifier only sees working -> idle/blocked, so a stalled role is otherwise invisible."
        },
        "loomSessionTracker.stallMinutes": {
          "type": "number",
          "default": 45,
          "minimum": 5,
          "description": "How long a working role may go without updating status.json before it is treated as stalled."
        },
        "loomSessionTracker.workingWarnThreshold": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "description": "Warn in the digest when this many roles are working simultaneously across ALL projects. They share one usage pool; Anthropic suggests 3-5 in parallel."
        },
        "loomSessionTracker.showAllProjects": {
          "type": "boolean",
          "default": false,
          "description": "Show every project's roles in this window instead of only this project's."
        },
        "loomSessionTracker.contextMemory": {
          "type": "boolean",
          "default": true,
          "description": "When the tagged orchestrator's context passes the threshold, have it write its working memory to a file, then clear its context and reload from that file. The clear is only sent after the file is verified on disk."
        },
        "loomSessionTracker.contextThresholdPct": {
          "type": "number",
          "default": 50,
          "minimum": 10,
          "maximum": 95,
          "description": "Percent of the context window at which the orchestrator is asked to bank its memory."
        },
        "loomSessionTracker.contextWindowTokens": {
          "type": "number",
          "default": 1000000,
          "minimum": 1000,
          "description": "Size of a session's context window, in tokens. Measured on this machine: auto-compaction fires at ~999,700 tokens, i.e. a 1M window \u2014 the model id alone does not tell you (sessions reporting 'claude-opus-5' reached 910k)."
        },
        "loomSessionTracker.contextMemoryFile": {
          "type": "string",
          "default": "",
          "description": "Where the orchestrator writes its working memory. Empty = ~/.claude/loom/<repo>/<role>/memory.md."
        },
        "loomSessionTracker.contextSaveTimeoutMinutes": {
          "type": "number",
          "default": 10,
          "minimum": 1,
          "description": "How long to wait for the memory file after asking for it. If it does not appear the cycle aborts and NOTHING is cleared."
        },
        "loomSessionTracker.contextClearTimeoutMinutes": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "description": "How long to wait for a fresh session to appear after /clear before giving up on the cycle (the memory file is already safe)."
        },
        "loomSessionTracker.contextCooldownMinutes": {
          "type": "number",
          "default": 15,
          "minimum": 0,
          "description": "Minimum time between context-memory cycles for one orchestrator."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "test": "./test.sh",
    "live": "./live.sh"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "@types/vscode": "^1.85.0",
    "@types/ws": "^8.5.10",
    "typescript": "^5.4.0"
  }
}
