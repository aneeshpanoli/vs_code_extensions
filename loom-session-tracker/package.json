{
  "name": "loom-session-tracker",
  "displayName": "Loom Session Tracker",
  "description": "Live per-project map of Loom session agents (role \u2194 webviewId) over CDP. Shows the orchestrator session so you can tag it in one click; tagged, it is auto-notified whenever a worker finishes.",
  "version": "0.38.4",
  "publisher": "local",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "loomSessionTracker",
          "title": "Loom Sessions",
          "icon": "$(broadcast)"
        }
      ]
    },
    "views": {
      "loomSessionTracker": [
        {
          "id": "loomSessions",
          "name": "Session Agents"
        }
      ]
    },
    "commands": [
      {
        "command": "loomSessionTracker.refresh",
        "title": "Loom Sessions: Refresh Now",
        "icon": "$(sync)"
      },
      {
        "command": "loomSessionTracker.status",
        "title": "Loom Sessions: Show Status"
      },
      {
        "command": "loomSessionTracker.spawn",
        "title": "Loom Sessions: Spawn Session\u2026",
        "icon": "$(add)"
      },
      {
        "command": "loomSessionTracker.retire",
        "title": "Loom Sessions: Retire (Close) Session",
        "icon": "$(close)"
      },
      {
        "command": "loomSessionTracker.delete",
        "title": "Loom Sessions: Delete Session (worktree + transcript)",
        "icon": "$(trash)"
      },
      {
        "command": "loomSessionTracker.lock",
        "title": "Loom Sessions: Lock (protect from deletion)",
        "icon": "$(lock)"
      },
      {
        "command": "loomSessionTracker.unlock",
        "title": "Loom Sessions: Unlock",
        "icon": "$(unlock)"
      },
      {
        "command": "loomSessionTracker.tagOrchestrator",
        "title": "Loom Sessions: Tag as Orchestrator",
        "icon": "$(star-full)"
      },
      {
        "command": "loomSessionTracker.untagOrchestrator",
        "title": "Loom Sessions: Untag Orchestrator",
        "icon": "$(star-empty)"
      },
      {
        "command": "loomSessionTracker.sessionCount",
        "title": "Loom Sessions: Show Simultaneous Session Count",
        "icon": "$(pulse)"
      },
      {
        "command": "loomSessionTracker.digest",
        "title": "Loom Sessions: What Needs Me? (startup digest)",
        "icon": "$(checklist)"
      },
      {
        "command": "loomSessionTracker.collectGarbage",
        "title": "Loom Sessions: Collect Garbage (across projects)"
      },
      {
        "command": "loomSessionTracker.toggleAllProjects",
        "title": "Loom Sessions: Toggle All-Projects View",
        "icon": "$(globe)"
      },
      {
        "command": "loomSessionTracker.worktreeReport",
        "title": "Loom Sessions: Worktree Cleanup Report",
        "icon": "$(trash)"
      },
      {
        "command": "loomSessionTracker.bankContext",
        "title": "Loom Sessions: Bank Orchestrator Memory & Clear Context",
        "icon": "$(save)"
      },
      {
        "command": "loomSessionTracker.reopenMissing",
        "title": "Loom: Reopen missing sessions (with memory)"
      },
      {
        "command": "loomSessionTracker.focusSession",
        "title": "Loom: Show this session's tab"
      },
      {
        "command": "loomSessionTracker.workLedgerReport",
        "title": "Loom: work ledger \u2014 what the agents actually produced",
        "icon": "$(graph)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "loomSessionTracker.spawn",
          "when": "view == loomSessions",
          "group": "navigation@1"
        },
        {
          "command": "loomSessionTracker.tagOrchestrator",
          "when": "view == loomSessions",
          "group": "navigation@2"
        },
        {
          "command": "loomSessionTracker.refresh",
          "when": "view == loomSessions",
          "group": "navigation@3"
        },
        {
          "command": "loomSessionTracker.sessionCount",
          "when": "view == loomSessions",
          "group": "navigation@4"
        },
        {
          "command": "loomSessionTracker.digest",
          "when": "view == loomSessions",
          "group": "navigation@5"
        },
        {
          "command": "loomSessionTracker.toggleAllProjects",
          "when": "view == loomSessions",
          "group": "navigation@6"
        },
        {
          "command": "loomSessionTracker.bankContext",
          "when": "view == loomSessions",
          "group": "navigation@7"
        },
        {
          "command": "loomSessionTracker.workLedgerReport",
          "when": "view == loomSessions",
          "group": "navigation@8"
        }
      ],
      "view/item/context": [
        {
          "command": "loomSessionTracker.lock",
          "when": "view == loomSessions && viewItem == loomAgent",
          "group": "inline@1"
        },
        {
          "command": "loomSessionTracker.retire",
          "when": "view == loomSessions && viewItem == loomAgent",
          "group": "inline@2"
        },
        {
          "command": "loomSessionTracker.unlock",
          "when": "view == loomSessions && viewItem == loomAgentLocked",
          "group": "inline@1"
        },
        {
          "command": "loomSessionTracker.lock",
          "when": "view == loomSessions && viewItem == loomAgent",
          "group": "1_actions"
        },
        {
          "command": "loomSessionTracker.unlock",
          "when": "view == loomSessions && viewItem == loomAgentLocked",
          "group": "1_actions"
        },
        {
          "command": "loomSessionTracker.retire",
          "when": "view == loomSessions && viewItem == loomAgent",
          "group": "1_actions"
        },
        {
          "command": "loomSessionTracker.tagOrchestrator",
          "when": "view == loomSessions && viewItem =~ /loomAgent/",
          "group": "1_actions"
        },
        {
          "command": "loomSessionTracker.untagOrchestrator",
          "when": "view == loomSessions && viewItem == loomOrchestrator",
          "group": "inline@1"
        },
        {
          "command": "loomSessionTracker.untagOrchestrator",
          "when": "view == loomSessions && viewItem == loomOrchestrator",
          "group": "1_actions"
        },
        {
          "command": "loomSessionTracker.delete",
          "when": "view == loomSessions && viewItem == loomAgent",
          "group": "2_destructive"
        },
        {
          "command": "loomSessionTracker.tagOrchestrator",
          "when": "view == loomSessions && viewItem == loomOwnerCandidate",
          "group": "inline@1"
        },
        {
          "command": "loomSessionTracker.tagOrchestrator",
          "when": "view == loomSessions && viewItem == loomOwnerCandidate",
          "group": "1_actions"
        }
      ]
    },
    "configuration": {
      "title": "Loom Session Tracker",
      "properties": {
        "loomSessionTracker.intervalMs": {
          "type": "number",
          "default": 15000,
          "minimum": 5000,
          "description": "How often (ms) to refresh the session-agent map. Clamped to a 5s floor."
        },
        "loomSessionTracker.notifyOrchestrator": {
          "type": "boolean",
          "default": true,
          "description": "When a worker's status.json goes working \u2192 idle/blocked, inject a 'check the outbox' prompt into the tagged orchestrator session."
        },
        "loomSessionTracker.sessionWarnThreshold": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "description": "Warn (status-bar highlight) when this many Claude conversations are open simultaneously across all windows. Anthropic sets no hard cap, but all sessions share one usage pool; its own guidance suggests 3-5 in parallel."
        },
        "loomSessionTracker.autoResumeAfterLimit": {
          "type": "boolean",
          "default": true,
          "description": "When a session is blocked by a usage limit, automatically nudge it to continue once the limit lifts (detected by the limit banner clearing, not by the UI's coarse ETA)."
        },
        "loomSessionTracker.resumeMessage": {
          "type": "string",
          "default": "",
          "description": "Prompt injected to resume a session after its usage limit resets. Empty = the built-in loom-resume message."
        },
        "loomSessionTracker.enforceWorkerModel": {
          "type": "boolean",
          "default": true,
          "description": "Keep the most expensive model for the orchestrator only. A worker found on a premium model is switched back with /model <workerModel>."
        },
        "loomSessionTracker.workerModel": {
          "type": "string",
          "default": "claude-opus-5",
          "description": "Model id workers are switched to (passed to /model). The orchestrator is exempt."
        },
        "loomSessionTracker.workerModels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "claude-opus-5",
            "claude-sonnet-5"
          ],
          "description": "The tiers a worker may be put on. A handoff chooses one per block by writing `model: <id>` into its inbox.md frontmatter; an id outside this list, or any premium id whatever this list says, is ignored and the worker stays on workerModel."
        },
        "loomSessionTracker.modelAckMs": {
          "type": "number",
          "default": 8000,
          "description": "How long a freshly spawned tab is given to acknowledge its /model switch before it is bound anyway. The bind is never skipped; an unswitched worker is corrected on the next idle tick."
        },
        "loomSessionTracker.premiumModels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "Fable 5.1",
            "Fable 5",
            "Mythos 5.1",
            "Mythos 5"
          ],
          "description": "Display names of the top pricing tier ($10/$50 per MTok) reserved for the orchestrator."
        },
        "loomSessionTracker.enforceOrchestratorModel": {
          "type": "boolean",
          "default": true,
          "description": "Keep the TAGGED orchestrator on the premium tier: when its own frame is seen idle on any other model it is switched with /model <orchestratorModel>. The mirror of enforceWorkerModel, for a default model pinned to the worker tier."
        },
        "loomSessionTracker.orchestratorModel": {
          "type": "string",
          "default": "claude-fable-5-1[1m]",
          "description": "Model id the tagged orchestrator is switched to (passed to /model) when it has not asked for a tier of its own in <repo>/orchestrator-model.json."
        },
        "loomSessionTracker.orchestratorModels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "claude-fable-5-1[1m]",
            "claude-opus-5",
            "claude-sonnet-5"
          ],
          "description": "The tiers an orchestrator may put ITSELF on by writing ~/.claude/loom/<repo>/orchestrator-model.json {\"model\": \"<id>\", \"reason\": \"<one line>\", \"at\": \"<iso>\"} (MS-001). An id outside this list is refused with a note and orchestratorModel stands. Enforced both directions, idle only, on the orchestrator's own frame; every performed self-shift appends a {self: true} line to model-ledger.jsonl."
        },
        "loomSessionTracker.showStartupDigest": {
          "type": "boolean",
          "default": true,
          "description": "On startup, summarise what needs attention: responses not picked up, roles blocked on a decision or by a usage limit, workers on the premium model, unbanked worktree changes, roles whose session is not open, and whether an orchestrator is tagged."
        },
        "loomSessionTracker.staleBusDays": {
          "type": "number",
          "default": 30,
          "minimum": 1,
          "description": "Flag a project bus as stale in the digest when nothing has touched it for this many days."
        },
        "loomSessionTracker.digestUnbankedCheck": {
          "type": "boolean",
          "default": true,
          "description": "Include a git status check of each role's worktree in the digest (runs at startup and on demand, not every tick)."
        },
        "loomSessionTracker.stallWatchdog": {
          "type": "boolean",
          "default": true,
          "description": "Warn (and tell the orchestrator) when a role has been 'working' with no status update for stallMinutes. The finish notifier only sees working -> idle/blocked, so a stalled role is otherwise invisible."
        },
        "loomSessionTracker.stallMinutes": {
          "type": "number",
          "default": 45,
          "minimum": 5,
          "description": "How long a working role may go without updating status.json before it is treated as stalled."
        },
        "loomSessionTracker.workingWarnThreshold": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "description": "Warn in the digest when this many roles are working simultaneously across ALL projects. They share one usage pool; Anthropic suggests 3-5 in parallel."
        },
        "loomSessionTracker.showAllProjects": {
          "type": "boolean",
          "default": false,
          "description": "Show every project's roles in this window instead of only this project's."
        },
        "loomSessionTracker.contextMemory": {
          "type": "boolean",
          "default": true,
          "description": "When the tagged orchestrator's context passes the threshold, have it write its working memory to a file, then clear its context and reload from that file. The clear is only sent after the file is verified on disk."
        },
        "loomSessionTracker.contextThresholdPct": {
          "type": "number",
          "default": 30,
          "minimum": 10,
          "maximum": 95,
          "description": "Percent of the context window at which the orchestrator is asked to bank its memory. 30 (was 50 until 2026-09-13): the context is re-read on every turn, so its length is the cost \u2014 one measured day was 2.93 billion cache-read tokens at 238k per turn. A restore costs ~30-60k."
        },
        "loomSessionTracker.contextWindowTokens": {
          "type": "number",
          "default": 1000000,
          "minimum": 1000,
          "description": "Size of a session's context window, in tokens. Measured on this machine: auto-compaction fires at ~999,700 tokens, i.e. a 1M window \u2014 the model id alone does not tell you (sessions reporting 'claude-opus-5' reached 910k)."
        },
        "loomSessionTracker.contextMemoryFile": {
          "type": "string",
          "default": "",
          "description": "Where the orchestrator writes its working memory. Empty = ~/.claude/loom/<repo>/<role>/memory.md."
        },
        "loomSessionTracker.contextSaveTimeoutMinutes": {
          "type": "number",
          "default": 10,
          "minimum": 1,
          "description": "How long to wait for the memory file after asking for it. If it does not appear the cycle aborts and NOTHING is cleared."
        },
        "loomSessionTracker.contextClearTimeoutMinutes": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "description": "How long to wait for a fresh session to appear after /clear before giving up on the cycle (the memory file is already safe)."
        },
        "loomSessionTracker.contextCooldownMinutes": {
          "type": "number",
          "default": 15,
          "minimum": 0,
          "description": "Minimum time between context-memory cycles for one orchestrator."
        },
        "loomSessionTracker.autoReopenOnRestart": {
          "type": "boolean",
          "default": true,
          "description": "After a window reload, reopen the role sessions that were live before it (from their freshest transcripts) and wake the orchestrator once it is back. The only case in which Loom opens tabs on its own."
        },
        "loomSessionTracker.maxActiveSessions": {
          "type": "integer",
          "default": 5,
          "minimum": 2,
          "description": "Sessions that may be OPEN at once in a project, including the orchestrator \u2014 so orchestrator + (n-1) agents. spawn() refuses past it. This is NOT ring.py's CAP, which limits how many sessions may be mid-turn simultaneously; concurrency is the memory cost, so raise that one separately and deliberately."
        },
        "loomSessionTracker.closeBlankShellsOnRestart": {
          "type": "boolean",
          "default": false,
          "description": "DISABLED and currently inert. 0.26.0 closed blank restored panels over CDP, which closes the whole editor window, not the tab (three windows lost, 2026-09-12). Kept only so a future tab-scoped implementation has a switch; setting it true does nothing today."
        },
        "loomSessionTracker.serveOpenRequests": {
          "type": "boolean",
          "default": true,
          "description": "Let this project's orchestrator open its own role sessions by writing ~/.claude/loom/<repo>/open-requests.json ({\"roles\":[...]}). Only roles on this project's roster, never an orchestrator, never one already live, never past the active-session cap, and a request older than 30 minutes is ignored. The file is replaced with the outcome."
        },
        "loomSessionTracker.gcEnabled": {
          "type": "boolean",
          "default": false,
          "description": "Collect garbage across projects. OFF for 0.33.0 on purpose: the first pass on a machine that has never had one moves hundreds of megabytes unattended, so run \"Collect Garbage (across projects)\" \u2192 Show plan \u2192 Run tiers 1+2 once by hand first. Once on: tier 1 (superseded extension builds no window is running, unreferenced transcripts older than gcTranscriptDays, backup files older than gcBackupDays) runs automatically every gcIntervalHours and only ever MOVES things into ~/.claude/loom/_archive/<date>/. Tier 2 (orphaned clean merged worktrees, board entries whose session has no transcript) needs your click. Tier 3 is only ever listed. Nothing is deleted, at any tier."
        },
        "loomSessionTracker.gcIntervalHours": {
          "type": "number",
          "default": 24,
          "description": "How often the automatic tier-1 pass runs, per MACHINE. One window at a time takes a lease in ~/.claude/loom/gc-state.json, so seven open windows do not each run it."
        },
        "loomSessionTracker.gcTranscriptDays": {
          "type": "number",
          "default": 14,
          "description": "A transcript is collectable only when it is older than this AND no bus references it (no board session_id, no context-state sessionId) AND it is not the newest transcript in its project directory. Measured 2026-09-13: 931 of 2.4 GB of transcripts had been untouched for 14 days."
        },
        "loomSessionTracker.gcBackupDays": {
          "type": "number",
          "default": 7,
          "description": "Archive *.bak-<epoch> files under ~/.claude/loom older than this many days."
        },
        "loomSessionTracker.workLedgerEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Measure what each project actually PRODUCED \u2014 from git and model-ledger.jsonl, never from what an agent wrote about itself \u2014 and show it as a `work ledger` node above that project's agents. Audited 2026-09-15: 10.7% of ReciEats' changed lines reached a user's screen over seven days and 171 of 614 commits only updated the guide, while every figure the panel showed came from the agents themselves and said the week went well."
        },
        "loomSessionTracker.productPaths": {
          "type": "object",
          "default": {
            "ReciEats": [
              "src/app/**",
              "src/lib/**"
            ],
            "pleodo": [
              "web/src/**",
              "engine/**",
              "blocks/**",
              "app/**"
            ]
          },
          "description": "Repo name \u2192 globs of what SHIPS to a user, for the shipsToUser figure. A repo not listed here falls back to a heuristic (everything except test/spec/scripts/tools/docs/*.md/lockfiles/config) and the panel says the figure is heuristic \u2014 an unconfigured guess presented as a measurement is the same lie in a new place. An EMPTY list means 'nothing here ships' and is honoured as configuration, not treated as absent."
        },
        "loomSessionTracker.excludePaths": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "**/*.test.*",
            "**/*.spec.*",
            "**/__tests__/**",
            "**/__mocks__/**"
          ],
          "description": "Globs SUBTRACTED from product whatever productPaths matches, and counted as rig instead. Kept as its own visible list rather than buried inside the product globs, so what was taken out can be seen. Measured 2026-09-15: without this, 39,150 of ReciEats' 63,225 'product' lines over seven days were test files living under src/ - src/app/page.test.tsx alone was +10,782, the single largest file in the product figure - and the shipping share read 56.8% instead of 25.1%. The '**/' prefixes matter: a bare '__tests__/**' anchors at the repo root and would miss src/__tests__/."
        },
        "loomSessionTracker.workLedgerWindowDays": {
          "type": "number",
          "default": 7,
          "minimum": 1,
          "description": "How many days of git history the work ledger measures."
        },
        "loomSessionTracker.workLedgerIntervalMin": {
          "type": "number",
          "default": 10,
          "minimum": 1,
          "description": "Minimum minutes between recomputes. The compute is ONE `git log --numstat` pass per repo, cached in ~/.claude/loom/<repo>/work-ledger.json with its computedAt; every tick in between reads that cache."
        },
        "loomSessionTracker.workLedgerThresholds": {
          "type": "object",
          "default": {
            "shipsGood": 40,
            "shipsBad": 20,
            "loopBackGood": 20,
            "loopBackBad": 50,
            "narrationGood": 10,
            "narrationBad": 25,
            "costPerLine": 0.25,
            "unshippedShareGood": 10,
            "unshippedShareBad": 50
          },
          "description": "Colour bands. Percentages: shipsToUser green \u2265shipsGood, red <shipsBad; loopBackRate and narrationShare invert (low is good), green \u2264Good, red >Bad. costPerLine is in DOLLARS of list-price equivalent per net product line \u2014 red above it. unshippedShare* is the PERCENTAGE of the window's net product that is not in front of a user (red at or above Bad, green at or below Good) \u2014 a share rather than a line count, because a fixed count made 13 unshipped lines read as a failure. Fields are read one by one, so a partial object still works."
        },
        "loomSessionTracker.modelPrices": {
          "type": "object",
          "default": {
            "claude-fable-5-1": [
              10,
              50,
              0.25,
              20
            ],
            "claude-fable-5": [
              10,
              50,
              1,
              20
            ],
            "claude-opus-5": [
              5,
              25,
              0.5,
              10
            ],
            "claude-opus-4-8": [
              5,
              25,
              0.5,
              10
            ],
            "claude-sonnet-5": [
              2,
              10,
              0.2,
              4
            ],
            "claude-haiku-4-5": [
              1,
              5,
              0.1,
              2
            ]
          },
          "description": "LIST price per model, [input, output, cacheRead, cacheWrite1h] in dollars per MTok, for the 'list-price equivalent' figure only \u2014 this work runs on a subscription and nobody is invoiced it. Verified 2026-09-15 against the published model table (Fable 5.1 $10/$50, Opus 5 $5/$25, Sonnet 5 $2/$10, Haiku 4.5 $1/$5; a cache read is 0.1x input and a 1-hour cache write is 2x, EXCEPT Claude Fable 5.1, whose cache reads are $0.25/MTok rather than $1.00). A model id with no row contributes its tokens to the token total but no dollars, and the panel names it rather than silently undercounting. Ids are matched by longest prefix, so a [1m] suffix or a dated snapshot inherits its family's row."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "test": "./test.sh",
    "live": "./live.sh"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "@types/vscode": "^1.85.0",
    "@types/ws": "^8.5.10",
    "typescript": "^5.4.0"
  }
}